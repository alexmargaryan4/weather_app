<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Показ уведомлений на Android 13+ (API 33) требует явного разрешения
         в рантайме — сам запрос делает NotificationService.requestPermission,
         но объявить разрешение в манифесте обязательно в любом случае. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Точное время ежедневной сводки (zonedSchedule с
         exactAllowWhileIdle в NotificationService) требует этого разрешения
         на Android 12+ (API 31+); без него система тихо понижает
         уведомление до неточного расписания с разбросом в десятки минут. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- Требуется Workmanager для периодической фоновой проверки погоды
         (WeatherWatcherTask) — сама задача уведомлена ограничением
         networkType: connected. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Позволяет перезапустить периодическую фоновую задачу после
         перезагрузки устройства — без него Workmanager возобновит её
         только при следующем открытии приложения. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Позволяет HomeWidgetLaunchIntent.getActivity(...) открывать
                 это Activity с URI, из которого мы читаем выбранный в
                 виджете город (см. WeatherWidgetProvider.launchAppIntent
                 и HomeScreen._cityKeyFromWidgetLaunch на Flutter-стороне). -->
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.LAUNCH"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Домашний виджет погоды. Один провайдер обслуживает все три
             размера (маленький/средний/большой) — Android сам выбирает,
             какой layout показать, через layout-w*dp-h*dp квалификаторы
             в res/layout, а не через отдельные провайдеры. -->
        <receiver
            android:name=".widget.WeatherWidgetProvider"
            android:exported="true"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.weather_app.ACTION_SELECT_CITY" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/weather_widget_info" />
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
