<?xml version="1.0" encoding="utf-8"?>
<!--
    Показывается вместо любого из трёх обычных layout'ов, пока для
    виджета ещё нет ни одной сохранённой записи о погоде (свежая
    установка, WidgetService.updateCity ещё ни разу не вызывался).
    Тап открывает приложение, где обычный поток загрузки погоды
    сам заполнит данные, и виджет обновится.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/widget_root"
    android:background="@drawable/widget_background">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="14dp"
        android:text="@string/widget_no_data"
        android:textColor="#D3DBEF"
        android:textSize="13sp"
        android:gravity="center" />
</FrameLayout>
